# Pinned listening set — fixed val songs followed by ear across ALL experiments
# (logged as wandb.Audio each eval). Shared config, not per-experiment.
# IDs resolve against manifests/songs.parquet (manifest = source of truth for paths/split);
# never walk folders. Edit this list to change the set — no code change needed.
listening_set:
  - 0631_민속악_판소리      # 판소리   — sparse, 소리북 + 6 melodic (no-vocal case)
  - 0854_창작국악_창작국악   # 창작국악 — dense heterophony, 15 stems (hard)
  - 0006_정악_풍류음악       # 풍류음악 — classic 10-instrument 정악 ensemble
  - 0721_민속악_민요         # 민요     — cleanest, tier-1 master≈Σ(stems)
  - 0196_민속악_산조         # 산조     — minimal 가야금 + 장구 (easy end)
