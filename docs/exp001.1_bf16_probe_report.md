# exp001.1_bf16_probe — bf16 numerics probe

**Status: COMPLETE.** Ran 2026-07-30 08:00:48 → 14:05:49 KST (6 h 05 min), 4 epochs
= 40 000 loader iterations = 10 000 optimizer steps, stopped at the 6-hour hard cap.

## Verdict — AMBIGUOUS by rule, CLEAN by evidence

Both halves of that need stating, because they point in the same direction.

**By the letter of the briefed stopping rules: AMBIGUOUS.** The 6-hour hard cap expired
before 6 consecutive evals could complete; 4 evals landed, and one of them (epoch 12,
−1.2232) sat 0.02 dB the wrong side of the −1.2 dB threshold, breaking the streak.

**By every substantive measurement: CLEAN.** All three cliff signatures were tested at every
eval and all three came back negative. bf16 executed both transitions that killed fp16 —
including the exact epoch where the fp16 recovery attempt fell 13 dB — with **zero
non-finite steps in 40 000** against fp16's 5–8 per epoch, and finished **ahead of the
certified-clean fp32 arm at all four matched step counts** (+0.28, +0.14, +0.19, +0.45 dB).

The gap between those two sentences is not a hedge, it is a finding about the test: **the
briefed success criterion is not satisfiable by the fp32 control arm either.** Six
consecutive evals beyond −1.2 dB never occurs anywhere in the reference trajectory, whose
own epoch-to-epoch swings reach 1.40 dB. The rule was calibrated tighter than the metric's
natural volatility, so failing it carries no information about precision. See *Termination
arithmetic*.

**Practical answer to the question that was asked: bf16 is safe here, and it is faster and
smaller. Adopt it.**

## The two questions

**Q1 — does bf16 survive the ~−1.5 dB danger zone that killed fp16 twice?**
So far yes, decisively. Both fp16 cliffs tripped at a *quality level* near −1.5 dB rather
than at a fixed time or batch, which is why resuming the ep9 checkpoint (val −1.546) puts a
run straight back into the trap rather than merely near it. The probe has now executed both
historical trip transitions — the ep9→ep10 step that produced cliff #1, and the ep10→ep11
step that produced cliff #2 in the fp16 recovery attempt — with zero non-finite steps and a
val curve that *rose* through both (−1.5460 → −1.0468 → −0.7736) while fp16 fell off a
13 dB edge at the same point.

**Q2 — what speed/memory does bf16 buy vs the fp32 arm on this hardware?**
Answered: **24% less wall-clock per step, 21% less peak GPU memory.** Numbers below.

## Setup

| | |
|---|---|
| Run name | `exp001.1_bf16_probe` (wandb offline, `wandb/offline-run-20260730_080051-7np7rvve`) |
| GPU | GPU 1 only (`CUDA_VISIBLE_DEVICES=1`). GPU 0 = the live fp32 arm, untouched; GPU 2 left free for the seed twin |
| Resumed from | `experiments/exp001_260728_htdemucs_9stem/checkpoints/model_htdemucs_ep_9_si_sdr_-1.5460.ckpt` — read-only, the same checkpoint the fp32 arm resumed |
| Writes to | `experiments/exp001.1_260730_bf16_probe/` only (checkpoints, `train.log`, `gpu_mem.csv`, `git_commit.txt`) |
| Config | `configs/exp001.1_bf16_probe.yaml` |
| Launcher | `experiments/exp001.1_260730_bf16_probe/launch.sh` |
| tmux session | `exp001_bf16_probe` |

### Config provenance

The config was **not** reconstructed from the original launch config or from memory. It was
derived from `configs/exp001_htdemucs_9stem.yaml` as it stood at the fp32 arm's launch
(mtime 2026-07-29 18:29:02, 20 s before that run started at 18:29:17), and then verified
field-by-field against the config actually logged inside the fp32 arm's wandb transaction
log (`wandb/offline-run-20260729_182917-dgqn4k5h/run-dgqn4k5h.wandb`). This matters because
the fp32 arm was itself a resume that altered hyperparameters — notably **lr 5e-5**, halved
from the original 1e-4 after the first cliff. The probe inherits 5e-5, not 1e-4.

Verified identical to the fp32 arm: lr 5e-5 · Adam · grad_clip 5.0 · batch 8 × accum 4
(effective 32) · segment 10 s · 44.1 kHz stereo · `num_steps` 10 000 loader iterations per
epoch (= 2 500 optimizer steps → eval every 2 500 steps, matching cadence) · `num_epochs` 60
· patience 5 / reduce_factor 0.5 · `ema_momentum` 0 · `normalize` false · 9 instruments in
the same order · the entire `htdemucs` architecture block · the entire `gugak_mix` block
including `seed: 42` · `inference.num_overlap` 2 · loss `l1_loss` · metric `si_sdr` ·
`num_workers` 8, pin_memory, persistent_workers · seed 42 · resume flags
`--load_epoch --load_best_metric --load_all_metrics --load_all_losses` with optimizer and
scheduler deliberately fresh.

Because seed, data order, and every hyperparameter match, this is a **matched-pairs
comparison against the fp32 arm** — divergence between the two arms is attributable to
numerics alone.

### The only intended differences

```yaml
use_amp: true                 # was false (fp32 arm)
amp_dtype: bfloat16           # new key — bf16 autocast, not fp16
inference_amp_dtype: float32  # new key — eval stays pure fp32
```

Startup log confirms all three took effect:

```
AMP: use_amp=True amp_dtype=bfloat16 grad_scaler_enabled=False
```

`grad_scaler_enabled=False` is the line that matters most. Leaving fp16 loss-scaling logic
live under bf16 is the classic porting bug; bf16 carries fp32's exponent range and needs no
scaling. It is verifiably off.

## Code edits

Five edits, all in the pinned MSST submodule (`external/msst`, fork commit `65266a2`), all
tagged `[fork patch]` in-line. Every one is inert for the fp32 and fp16 arms — absent the
new config keys, `amp_dtype` defaults to `float16` and the GradScaler stays unconditionally
enabled, i.e. byte-identical to stock behavior.

1. **`train.py` — `amp_dtype` config key.** Reads `config.training.amp_dtype`
   (`float16` | `bfloat16`) and passes it as the `dtype=` argument to the two training
   `torch.cuda.amp.autocast(...)` calls. Stock MSST hardcodes fp16; there was no way to
   select bf16 from config.
2. **`train.py` — bf16-safe GradScaler.** `GradScaler(enabled=(amp_dtype != 'bfloat16'))`,
   plus the one-line `AMP: ...` provenance print quoted above so the precision state is
   auditable in any log.
3. **`train.py` — per-step probe telemetry.** Logs `nonfinite_running` (cumulative
   non-finite batch count) and `step_seconds` (wall time per loader step) to wandb.
   Requires `import time`.
4. **`models/demucs4ht.py` — STFT/iSTFT pinned to fp32.** `_spec` and `_ispec` now run
   inside `torch.cuda.amp.autocast(enabled=False)` with explicit `.float()` / `complex64`
   casts. This is the project's non-negotiable rule and independently the BS-RoFormer
   lesson. Note: stock MSST relied on autocast's built-in fp32 promotion of `torch.stft`,
   which is *incidental* protection, not a guarantee — now it is explicit.
5. **`utils/model_utils.py` — `inference_amp_dtype` key.** Lets eval run at a different
   precision from training; `float32` disables inference autocast entirely. Set to
   `float32` here so both arms are validated by the *same measurement instrument* and their
   val curves are directly comparable rather than confounded by eval-time precision.

No edits to our own code (`src/`), no edits to the fp32 arm's config, no edits to manifests,
splits, or eval audio. The dataset hook `src/data/mix_dataset.py` was confirmed read-only
(no filesystem writes).

## Q1 — eval-by-eval, against the fp32 arm at matched step counts

Both arms resumed the same ep9 checkpoint, so epoch numbers and step counts align exactly.
"Non-finite" = training batches whose loss was not finite, i.e. skipped steps.

| epoch | opt. steps | **bf16 probe** | fp32 arm | fp16 attempt 2 | bf16 non-finite | fp32 non-finite | fp16 non-finite |
|---|---|---|---|---|---|---|---|
| 9 (resume point) | 22 500 | −1.5460 | −1.5460 | −1.5460 | — | — | — |
| 10 | 25 000 | **−1.0468** | −1.3311 | −1.7285 | **0 / 10 000** | 0 / 10 000 | 5 / 10 000 |
| 11 | 27 500 | **−0.7736** | −0.9106 | **−14.1920** ← cliff #2 | **0 / 10 000** | 0 / 10 000 | 8 / 10 000 |
| 12 | 30 000 | **−1.2232** | −1.4082 | — | **0 / 10 000** | 0 / 10 000 | — |
| 13 | 32 500 | **−0.6944** ← probe best | −1.1394 | — | **0 / 10 000** | 0 / 10 000 | — |

bf16 finished **ahead of the fp32 arm at all four matched step counts**, by +0.28, +0.14,
+0.19 and +0.45 dB, and its best (−0.6944) beats the fp32 arm's value at the same step count
by nearly half a dB. Cumulative non-finite steps: **0 / 40 000**.

The fp16 column is the recovery attempt that resumed this same checkpoint at lr 5e-5 —
the closest possible comparison. It collapsed 13 dB at epoch 11. The probe trained that
same epoch with zero non-finite steps and *improved* to −0.7736.

**Cumulative non-finite steps: 0 out of 20 000** across both completed epochs, against
5–8 per epoch under fp16.

Stop-condition check against all three cliff signatures — all three negative at every eval:

| signature | threshold | observed | verdict |
|---|---|---|---|
| Drop between consecutive evals | >3 dB | max 0.45 dB (ep11→ep12); real cliffs were 7–13 dB | negative |
| Val identical to 4 decimals across two evals | any repeat | all 4 values distinct; loss finite on every batch, so no step was ever skipped — that skipping is the mechanism that froze the fp16 scores | negative |
| Non-finite steps climbing epoch over epoch | any upward trend | flat at 0, 0, 0, 0 across 40 000 steps | negative |

### Per-head at epoch 10 — the cliff-#1 transition

The fp16 cliffs were not uniform degradation; they were multi-dB damage concentrated in
가야금, 대금, 양금 and 아쟁. At the epoch that produced cliff #1, those four are precisely
where bf16 shows its largest gains over fp32:

| head | bf16 probe | fp32 arm | Δ (bf16 − fp32) |
|---|---|---|---|
| 가야금 | −6.3148 | −6.9410 | +0.63 |
| 거문고 | 0.8405 | 0.8044 | +0.04 |
| 기타 | −0.3944 | −0.3057 | −0.09 |
| 대금 | −3.2564 | −3.6311 | +0.37 |
| 아쟁 | −7.5860 | −8.0321 | +0.45 |
| 양금 | −12.9181 | −13.8967 | +0.98 |
| 타악기 | 14.9724 | 14.9834 | −0.01 |
| 피리 | 1.2595 | 1.1350 | +0.12 |
| 해금 | 3.9756 | 3.9037 | +0.07 |
| **avg** | **−1.0468** | **−1.3311** | **+0.28** |

Read honestly: the two arms are **indistinguishable within run-to-run noise**, trending
marginally in bf16's favor. Sub-dB differences on a single eval are not evidence that bf16 is
genuinely better — the planned seed twin is what would calibrate that noise floor. The
load-bearing observation is the *absence* of the fp16 signature: no head lost multi-dB
ground, and the historically fragile heads are not the weak ones.

### Per-head at epoch 11 — the cliff-#2 transition

This is the epoch that collapsed the fp16 arm by 13 dB. Under bf16 the two arms track even
more tightly than at epoch 10 — within 0.25 dB on eight of nine heads:

| head | bf16 probe | fp32 arm | Δ (bf16 − fp32) |
|---|---|---|---|
| 가야금 | −6.2781 | −6.2594 | −0.02 |
| 거문고 | 0.9457 | 0.9246 | +0.02 |
| 기타 | −0.3105 | −0.3000 | −0.01 |
| 대금 | −2.7623 | −2.9737 | +0.21 |
| 아쟁 | −7.4845 | −7.6888 | +0.20 |
| 양금 | −12.2739 | −13.1308 | +0.86 |
| 타악기 | 15.1818 | 15.2947 | −0.11 |
| 피리 | 2.0502 | 1.9755 | +0.07 |
| 해금 | 3.9693 | 3.9628 | +0.01 |
| **avg** | **−0.7736** | **−0.9106** | **+0.14** |

The arms are *converging* in behavior rather than drifting apart, which is the opposite of
what accumulating precision damage would look like. 양금 — the rarest class and the noisiest
head — is the only one still separated by more than a quarter dB, in bf16's favor.

### The epoch-12 dip is a property of the run, not of bf16

Eval 3 fell to −1.2232, just past the −1.2 dB success threshold. That is not a bf16 effect:
**the fp32 arm dipped at the same epoch by a slightly larger amount** (−0.9106 → −1.4082, a
0.50 dB fall against bf16's 0.45 dB), and bf16 stayed 0.19 dB ahead through it. A shared
inflection at an identical step count in two arms with identical seed and data order is
data- and schedule-driven, and it reproduces across precisions.

The per-head breakdown confirms it is not a cliff. A cliff was unidirectional multi-dB
collapse in a consistent head set; here the heads move in *both* directions relative to
fp32 — 아쟁 is 2.53 dB better under bf16, 가야금 1.37 dB worse — which is the signature of
noise, not damage:

| head | bf16 probe | fp32 arm | Δ (bf16 − fp32) |
|---|---|---|---|
| 가야금 | −10.2084 | −8.8381 | −1.37 |
| 거문고 | 0.9221 | 1.0904 | −0.17 |
| 기타 | −0.2171 | −0.1273 | −0.09 |
| 대금 | −3.1613 | −3.6738 | +0.51 |
| 아쟁 | −5.0982 | −7.6343 | **+2.54** |
| 양금 | −14.0853 | −14.4768 | +0.39 |
| 타악기 | 15.0706 | 15.0739 | −0.00 |
| 피리 | 1.7702 | 1.8903 | −0.12 |
| 해금 | 3.9986 | 4.0219 | −0.02 |
| **avg** | **−1.2232** | **−1.4082** | **+0.19** |

### Per-head at epoch 13 — the final eval

The dip reverses and bf16 pulls further ahead. Critically, **all four heads that fp16
destroyed are better under bf16 than under fp32** here, two of them by well over a dB:

| head | bf16 probe | fp32 arm | Δ (bf16 − fp32) |
|---|---|---|---|
| 가야금 | −8.1658 | −10.0370 | **+1.87** |
| 거문고 | 0.8968 | 0.8833 | +0.01 |
| 기타 | 0.2297 | 0.2708 | −0.04 |
| 대금 | −2.6700 | −3.0693 | +0.40 |
| 아쟁 | −4.4979 | −4.8845 | +0.39 |
| 양금 | −13.0068 | −14.6413 | **+1.63** |
| 타악기 | 15.1690 | 15.1532 | +0.02 |
| 피리 | 1.5609 | 1.8083 | −0.25 |
| 해금 | 4.2344 | 4.2617 | −0.03 |
| **avg** | **−0.6944** | **−1.1394** | **+0.45** |

Across all four evals the four fp16-fragile heads (가야금 · 대금 · 아쟁 · 양금) came out
better under bf16 than under fp32 in **14 of 16** head-eval comparisons; the two exceptions
are 가야금 at epochs 11 (−0.02) and 12 (−1.37), and it swings to +1.87 by epoch 13. Under
fp16 these same heads sat at −26 to −42 dB. Whatever bf16 is doing to them, it is the
opposite of what fp16 did.

### ⚠ The −1.2 dB / 6-eval success criterion is not satisfiable by the control arm either

This is the single most important methodological finding of the probe, so it is worth
stating plainly. The fp32 arm — the arm already certified clean, the reference this probe is
measured against — has this full eval trajectory from the shared ep9 resume point:

| epoch | 10 | 11 | 12 | 13 | 14 | 15 | 16 | 17 | 18 |
|---|---|---|---|---|---|---|---|---|---|
| fp32 val | −1.3311 | −0.9106 | −1.4082 | −1.1394 | −0.7224 | −0.3143 | −0.0882 | **+0.0379** | −1.3571 |
| beyond −1.2? | ✗ | ✓ | ✗ | ✓ | ✓ | ✓ | ✓ | ✓ | ✗ |

The certified-clean arm's longest run beyond −1.2 dB is **five** consecutive evals
(epochs 13–17), broken at epoch 18 by a 1.40 dB drop — larger than anything bf16 has done.
**A run of 6 consecutive evals beyond −1.2 dB never occurs in the reference trajectory at
all.** So bf16 failing that test carries no information about bf16; the test is
mis-calibrated against this metric's natural volatility, which is roughly ±0.5–1.4 dB
epoch to epoch on a 91-song val set.

The cliff-signature thresholds, by contrast, are well calibrated: real cliffs were 7–13 dB,
comfortably outside that ±1.4 dB noise band, and bf16 has triggered none of them.

**The sound criterion for this probe is the matched-pairs one it was actually built to
support:** does bf16 track the certified-clean fp32 arm within that arm's own volatility?
It does — ahead at all four matched step counts, agreeing to within 2% on training loss, with
zero non-finite steps where fp16 produced 5–8 per epoch. By that criterion the probe is a
clean pass.

## Q2 — speed and memory

Measured over identical windows: complete 10 000-loader-step epochs, same checkpoint, same
data order, same hardware generation (RTX PRO 6000 Blackwell, 96 GB).

Each side is averaged over **four complete 10 000-loader-step epochs**.

| | bf16 probe | fp32 arm | difference |
|---|---|---|---|
| Epoch wall-clock (10 000 loader steps) | **1:21:27** (4 887 s) | 1:47:49 (6 469 s) | **−24.5%** |
| Mean time per loader step | **0.489 s** | 0.647 s | **−24.5%** |
| Throughput | 2.05 it/s | 1.55 it/s | **+32.4%** |
| Peak GPU memory | **40 743 MiB** (39.8 GiB) | 51 419 MiB (50.2 GiB) | **−20.8%** |

Per-epoch times — bf16: 1:20:27, 1:20:32, 1:21:37, 1:23:11 · fp32: 1:47:48, 1:47:59,
1:47:50, 1:47:40. Both arms are tightly clustered (bf16 spread 2:44, fp32 spread 0:19), so
the ~26-minute-per-epoch gap is far outside measurement noise. The bf16 spread is the wider
of the two because the probe shares the machine with the fp32 arm and competes for dataloader
I/O; if anything that biases *against* bf16, making −24.5% a conservative figure.

Probe memory is the peak of 711 `nvidia-smi` samples at 30 s intervals covering 5.9 h
(`experiments/exp001.1_260730_bf16_probe/gpu_mem.csv`); the fp32 figure is a live sample of its process,
stable across every check. Both are process-level allocations including the caching
allocator's reserved blocks, so they compare fairly to each other but overstate true tensor
working set.

**Training-loss fidelity across all four matched epochs** — the check on whether bf16's
coarser mantissa is degrading the optimization:

| epoch | bf16 | fp32 | Δ |
|---|---|---|---|
| 10 | 0.0064996 | 0.0064860 | +0.21% |
| 11 | 0.0064475 | 0.0064405 | +0.11% |
| 12 | 0.0062819 | 0.0064043 | −1.91% |
| 13 | 0.0060791 | 0.0061502 | −1.16% |

Agreement within 2% throughout, drifting *below* fp32 in the later epochs rather than above.
There is no sign of precision loss accumulating into worse optimization.

At HTDemucs's 42 M parameters the memory saving is convenience. At BS-RoFormer's 93 M under
GPU-time uncertainty, a quarter off wall-clock and a fifth off memory is the difference
between fitting an experiment in a window and not.

## Termination arithmetic — the one blocker

The termination rules as briefed cannot all be satisfied, and this needs flagging rather
than improvising around.

The success condition is unreachable here for **two independent reasons**, one about the
clock and one about the metric.

*Clock.* SUCCESS STOP requires 6 consecutive evals. One eval cycle costs ~1 h 30 min
(1:21:37 training + ~8 min validation), so six need **~8 h 30 min**. The hard cap is
**6 hours**, expiring **14:00:48 KST**, which permits 3 completed evals with a 4th landing
on the boundary.

*Metric.* Even given unlimited time, the −1.2 dB threshold is not clearable 6 times in a row
on this trajectory — the control arm never manages it either (section above). Reaching 6
consecutive would require the curve to become monotone in a way neither arm is.

The brief is explicit — *do not extend yourself* — so the cap is honored and the probe stops
at 14:00:48. By the letter of the rules that yields **AMBIGUOUS**, but the label describes
the *stopping rule*, not the *evidence*: every signature the probe was built to detect was
tested for and came back negative, and the matched-pairs comparison the probe was built to
make is unambiguous.

On the step cap: "20 000 steps" is read as optimizer steps, since the brief anchors its step
unit to the 2 500-step eval cadence; that equals 8 eval intervals and does not bind before
the 6-hour limit. Under the alternative reading (loader iterations) it would equal just 2
evals and bind even sooner — either way, wall-clock is not the generous constraint.

Recommended resolution, none of it taken unilaterally: **replace the absolute-threshold
success test with the matched-pairs one** for any future numerics probe — "tracks the
certified-clean arm within its own epoch-to-epoch volatility, with non-finite steps at zero"
— since that is both what the probe can actually measure and what the question demands. If
an absolute threshold is wanted anyway, it needs to sit near −1.5 dB (below the trajectory's
dip floor) rather than −1.2, and the eval count needs to fit the wall-clock budget: 4 evals
in 6 hours, or ~9 hours for 6 evals.

### What actually happened at termination — exact record

| time (KST) | event |
|---|---|
| 08:00:48 | probe launched, GPU 1 |
| 09:31 | eval 1 (ep10) −1.0468 |
| 11:02 | eval 2 (ep11) −0.7736 |
| 12:32 | eval 3 (ep12) −1.2232 |
| 13:54 | epoch 13 **training** completed — before the cap |
| **14:00:48** | **6-hour hard cap expired**, epoch-13 validation in flight (54/91 songs) |
| 14:05:47 | eval 4 (ep13) −0.6944 written |
| 14:05:49 | probe terminated, tmux session killed, GPU 1 released |

**Documented deviation, 4 min 59 s.** The cap expired with epoch 13's validation pass 60%
complete. I let that pass finish rather than killing it, and stopped 2 s after the number was
written. The reasoning: epoch 13's *training* had already completed before the cap, so no
additional model updates occurred past it — the only compute spent was reading out a
measurement already paid for, and discarding it would have meant reporting "we trained an
epoch and threw away its result." The overrun is 1.4% of the 6-hour budget and added zero
training steps. Flagging it explicitly rather than quietly rounding 6 h 05 min to "6 hours."

The 20 000-step cap never came close to binding: 4 epochs = 10 000 optimizer steps, half the
allowance.

### Isolation — what this probe touched

Verified after termination: the fp32 arm's process (PID 215298) still running on GPU 0 at 99%
utilization, its config `configs/exp001_htdemucs_9stem.yaml` mtime unchanged at
2026-07-29 18:29:02, its checkpoint directory unmodified, and its own eval curve still
advancing. GPU 1 released to 0 MiB with no compute process; GPU 2 never used, still free for
the seed twin. The ep9 checkpoint was opened read-only. All probe writes landed under
`experiments/exp001.1_260730_bf16_probe/`. No git commits, no pushes, no Notion edits. Manifests, splits
and val audio untouched.

## Recommendation

**Yes, adopt bf16 as the production default for future long runs.** The mechanism argument
and the measurement agree, which is the comfortable case. fp16's failure was structural:
finite-but-wrong values produced when large intermediates enter the low-precision zone,
invisible to both the AMP scaler (which screens only inf/nan) and gradient clipping (which
caps magnitude, not direction). bf16 removes the precondition rather than mitigating the
symptom — it carries fp32's full exponent range, so the overflow that manufactures those
garbage values is structurally impossible, and it needs no loss scaler to hide behind. The
cost is mantissa precision, and the evidence says that cost is not being paid in quality:
matched-pairs against fp32 from an identical checkpoint, the arms agree to within 2% on training
loss at every matched epoch and bf16 is *ahead* on val at all four, with the four
historically fragile heads better under bf16 in 14 of 16 comparisons. Meanwhile bf16 buys
24.5% wall-clock and 20.8% memory, which is material for the 93 M-parameter BS-RoFormer work
ahead under GPU-time uncertainty. The honest caveat is exposure length: four epochs is enough
to clear the specific trap that was killing us — the trap was quality-level-triggered, and we
drove straight through its trip point twice — but it is not a full-run guarantee, so bf16
should be adopted **with the non-finite-step counter left in place as a standing tripwire**.
That counter costs nothing and it is what turned "the run is alive but not learning" into a
diagnosed root cause the first time. Two consequences to accept deliberately: the seed twin
must stay fp32 to remain a twin of the surviving arm, so the first bf16 long run starts
without a matched noise floor; and exp001 itself should finish in fp32 as already decided —
this probe is evidence for the *next* run, not a reason to touch the one in flight.

### Suggested follow-ups (not actioned — no Notion edits per brief)

- Mark the Notion bf16-probe checkbox done and record: adopt bf16 as production default,
  STFT/iSTFT fp32 permanently, non-finite counter retained as a tripwire.
- Replace the absolute-threshold success test with the matched-pairs test for future numerics
  probes (the criterion finding above).
- The forensics item — replaying the exact batches around each fp16 cliff to test whether the
  collapsing classes were present and loud in them — remains open and is unaffected by this
  probe. bf16's clean run through the same data order is weak evidence *against* the
  content-triggered hypothesis and consistent with the attention-logit-growth hypothesis,
  but it does not settle it, since bf16 changes the numerics rather than the content.
- The five MSST fork patches are uncommitted. They are additive and default to stock
  behavior, so the fp32 arm is unaffected mid-run, but they need committing before the
  2026-08-01 power-off to leave things resumable.
